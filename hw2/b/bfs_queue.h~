#include <iostream>
#include <string>
#include <sstream>
#include <algorithm>
#include <iterator>
#include <vector>
#include <queue>
#include <cstdlib>
#include <algorithm>
#include <cilk.h>

using namespace std;

class BfsQueue {
  vector<queue<int> > queues;
  int nseg;
  int size;
  int p;
  int seg;
  cilk::mutex m;

public:
  BfsQueue(int p);
  void enque(int i, int value);
  vector<int> * deque();
  void set_segment_size();
  
};

